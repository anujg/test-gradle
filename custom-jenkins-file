#!groovy

pipeline {
    agent {label 'docker&&ubuntu1604&&language:java'}
    stages{
        stage('Build project') {
            steps {
                sh './buildscripts/build.sh'
            }
        }
    }
}
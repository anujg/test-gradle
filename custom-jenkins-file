#!groovy

pipeline {
    agent {label 'stable&&docker&&ubuntu1604&&sudo&&"language:java"&&env&&jdk&&mykerio'}
    stages{
        stage('Build project') {
            steps {
                sh './buildscripts/build.sh'
            }
        }
    }
}
#!groovy

pipeline {
    environment {
        SECRET1 = "New-secret"
    }
    agent {label 'docker&&ubuntu1604&&language:java'}
    stages{
        stage('Build project') {
            steps {
                script {
                    sh './buildscripts/build.sh'
                }
            }
        }
    }
}
#!groovy

pipeline {
    withCredentials([string(credentialsId: 'SECRET1', variable: 'SECRET1')]) {
        environment {
            SECRET1 = "$SECRET1"
        }
    }
    agent {label 'docker&&ubuntu1604&&language:java'}
    stages{
        stage('Build project') {
            steps {
                script {
                    sh './buildscripts/build.sh'
                }
            }
        }
    }
}